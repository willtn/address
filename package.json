{
  "name": "address",
  "version": "0.0.1",
  "description": "Address for Student Benefit",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf build/**/* && tsc -p src/tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "rimraf dist/**/* && npm run build && cp -r build/* dist/"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.pdx.groowin.com:trung.nguyen/address.git"
  },
  "author": "Trung Nguyen",
  "private": true,
  "devDependencies": {
    "rimraf": "^2.6.1",
    "typescript": "^2.6.1"
  },
  "dependencies": {
    "funfix-core": "^6.2.2"
  }
}
